pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python3 --version'
                sh 'pwd'
                sh 'echo some console output'
            }
        }
    }
}
