pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'pwd'
                sh 'echo some console output'
            }
        }
    }
}
