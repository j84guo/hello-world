pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh -c 'python' '--version'
                sh -c 'pwd'
                sh -c 'echo some console output'
            }
        }
    }
}
