pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'pwd'
                sh 'whoami'
                sh 'echo some console output'
            }
        }
    }
}
