pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                python --version
                pwd
                echo 'some console output...'
            }
        }
    }
}
